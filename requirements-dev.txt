# Data versioning (Milestone 0)
dvc==3.55.2
dvc-s3==3.2.0  # S3-compatible remote for DagsHub storage (dvc push/pull)
dagshub==0.3.35
pathspec==0.12.1  # pin: dvc 3.55.2 is incompatible with pathspec>=1.0 (_DIR_MARK import error)

# Test + lint (Milestone 3)
pytest==8.2.2
ruff==0.5.0
httpx==0.27.0          # FastAPI TestClient dependency (Milestone 4)
